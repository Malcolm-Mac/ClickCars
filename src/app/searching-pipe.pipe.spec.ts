import { SearchingPipePipe } from './searching-pipe.pipe';

describe('SearchingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
